[package]
name = "tui"
version = "0.2.0"
edition = "2021"

[dependencies]
crossterm = { workspace = true }
ratatui = { workspace = true }
config = { path = "../config" }

[features]
interactive = []  
inline = []
